import { DropDpwnToggleDirective } from './drop-dpwn-toggle.directive';

describe('DropDpwnToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDpwnToggleDirective();
    expect(directive).toBeTruthy();
  });
});
